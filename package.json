{
  "name": "quiz-server",
  "version": "2.0.0",
  "description": "Quiz backend for Whitestone Insurance agent training",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  }
}
